import "<local>/thing.az";
import "<local>/io.az";


def print_int(num: int32) -> int32 {
    if (num == 0) {
        io::putchar(48);
    } else {
        if (num < 0) {
            io::putchar(45);
            num = num * -1;
        }
        var original: int32 = num;
        var digits: int32 = 0;
        while (num > 0) {
            digits = digits + 1;
            num = num / 10;
        }
        while (digits > 0) {
            var digit: int32 = original;
            var i: int32 = 0;
            while (i < (digits-1)) {
                digit = digit / 10;
                i = i + 1;
            }
            digit = digit % 10;
            io::putchar(48 + digit);
            digits = digits - 1;
        }
    }
    io::putchar(10);
    return 0;
}


@entry
def main() -> int32 {
    print_int(123423);
    return 0;
}