cimport std:"aizeio.h" std:"aizeio.c";


def fibo(n: int) -> int {
    if (n < 2) {
        return n;
    } else {
        return fibo(n-1) + fibo(n-2);
    }
}


def fast_fibo(n: int) -> int {
    var f0: int = 0;
    var f1: int = 1;
    var f2: int = f0 + f1;
    var i: int = 0;
    while (i < n) {
        f2 = f0 + f1;
        f0 = f1;
        f1 = f2;
        i = i + 1;
    }
    return f0;
}


def main() -> int {
    aizeio::print_int(fast_fibo(39));
    aizeio::print_int(fibo(39));
    return 0;
}
