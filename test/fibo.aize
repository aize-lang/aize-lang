import std:"io";

def fibo(n: int) -> int {
    if (n < 2) {
        return n;
    } else {
        return fibo(n-1) + fibo(n-2);
    }
}


def main() -> int {
    var i: int = 0;
    while (i < 21) {
        io::print_int(fibo(i), 1);
        i = i + 1;
    }
    return 0;
}