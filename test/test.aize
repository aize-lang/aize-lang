cimport std:"aizeio.h" std:"aizeio.c";
import std:"io";


def fibo(n: long) -> long {
    if (n < 2) {
        return n;
    } else {
        return fibo(n-1) + fibo(n-2);
    }
}


def main() -> int {
    {
        var i: int = 0;
        while (i < 30) {
            io::print_int(fibo(i));
            i = i + 1;
        }
    }
    return 0;
}

