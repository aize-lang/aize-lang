# import std:"io";


class Person {
    attr age: int;
    attr weight: int;

    method copy(this: Person) -> Person {
        return Person::new(this.age, this.weight);
    }
}


def main() -> int {
    var person = Person::new(10, 10);
    var clone = person.copy();
    return 0;
}