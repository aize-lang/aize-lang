# import std:"io";


trait Sized {
    method length(this: Sized) -> int;
}


class Stick : Sized {
    attr size: int;

    method length(this: Stick) -> int {
        return this.size;
    }
}


def main() -> int {
    var stick = Stick::new(10);
    stick.length();
    return 0;
}